//! High-level API exposed for Flutter / FFI bridge generation.
//!
//! Only functions and types in this module are exposed to flutter_rust_bridge.
//! Internal implementations remain encapsulated within semantic/ and hybrid/.

pub mod hybrid_search;
