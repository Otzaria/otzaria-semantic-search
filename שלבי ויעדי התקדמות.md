# שלבי ויעדי התקדמות — חיפוש סמנטי באוצריא

> צילום מצב: 6 באוגוסט 2026.
>
> מסמך זה מעדכן את `מפת דרכים.md` לאחר מיזוג PR #3, PR #12 ו־PR #669. הוא אינו טוען שקוד אב־טיפוס הוא כבר מסלול production.
>
> **סטטוס:** S0 בוצע, וחלק הזהות של S3 בוצע. חוזה המוצר נמצא ב־
> [`docs/PRODUCT_CONTRACT.md`](docs/PRODUCT_CONTRACT.md) וחוזה הארטיפקט ב־
> [`docs/ARTIFACT_CONTRACT.md`](docs/ARTIFACT_CONTRACT.md); הם המסמכים המחייבים בכל סתירה.
>
> **סדר העבודה שנבחר אינו סדר המספור.** S1 (איכות הייצוג) נדחה, והעבודה מתקדמת
> ב„תשתית תחילה”: מניחים שיש מודל ווקטורים מוכנים, ובונים את הצנרת שתחבר אותם לתוכנה.
> זה אפשרי דווקא מפני שהממד, הדיוק ופורמט ה־store הם **נתונים בתוך ה־manifest** ולא
> קבועים בקוד — הכרעות S1/S2 ימלאו שדות קיימים ולא ישנו את החוזה.
>
> הבא בתור: **S2a** — מסלול ריצה read-only שפותח ארטיפקט מאומת. אחריו S4a (packer
> לווקטורים מוכנים), S5, S6 ו־S7. S1 ו־S2b (סקייל, ANN, benchmark) חוזרים לפני יצירת
> הארטיפקט האמיתי והכרעת backend ה־production.

## 1. היעד וגבולות המוצר

היעד הוא חיפוש `lexical` / `hybrid` / `semantic` באוצריא, פרטי ומקומי בזמן השימוש.

החלטות ההיקף הנוכחיות:

- אין שירות ענן בזמן החיפוש, ושאילת המשתמש אינה נשלחת לשרת.
- וקטורי הספרייה הרשמית נוצרים מראש במכונת build/CI ומופצים כארטיפקט סטטי. המשתמש אינו מאנדקס את הספרייה.
- הביטוי „ענן” בקוד הקיים מתאר אריזה/ייבוא בלבד. ✅ **בוצע ב־S0:** המודול הוא `distribution`.
- מודל ה־Embedding נדרש מקומית רק כדי להפוך שאילתה חדשה לווקטור. הוא חייב להיות זהה למודל שיצר את וקטורי הספרייה.
- אין בשלב זה אינדוקס או חיפוש סמנטי בספרים האישיים של המשתמש.
- Tantivy נשאר מקור האמת לטקסט, metadata, facets, BM25 והצגת התוצאה. האינדקס הווקטורי הוא sidecar קריא בלבד.
- שני מנועי החיפוש נארזים באותה ספרייה native של `otzaria_search_engine`; אין צורך ב־executable נפרד באפליקציה.

## 2. תמונת המצב בשלושת המאגרים

| מאגר | מצב נוכחי | מה עובד | מה עוד לא במסלול production |
|---|---|---|---|
| `otzaria-semantic-search` | `main` אחרי מיזוג S0; [PR #3](https://github.com/Otzaria/otzaria-semantic-search/pull/3) מוזג | inference אמיתי, hybrid/fusion, profiles, cache, telemetry, אב־טיפוס persistence, אריזה, וחוזה זהות ארטיפקט מאומת | ה־engine הפעיל עדיין משתמש ב־`VectorStore` בזיכרון ואינו פותח ארטיפקט מאומת; אין ANN/mmap, אין builder של הספרייה הרשמית ואין benchmark בקנה מידה מלא |
| `otzaria_search_engine` | `refactor` ב־`8a9b7e1`; release 0.7.0; [PR #12](https://github.com/Otzaria/otzaria_search_engine/pull/12) מוזג | FFI אמיתי, מועמדי BM25 מ־Tantivy, fusion ב־Rust, hydration לפי `line_id`, סינון ובדיקות מקצה לקצה | נעול ל־semantic revision `d9de678` שקדם ל־PR #3; אין API לפתיחת אינדקס רשמי מוכן; API האינדוקס הנוכחי מקבל כל שורה מ־Dart |
| `otzaria` | `dev` כולל את [PR #669](https://github.com/Otzaria/otzaria/pull/669); תלות `otzaria_search_engine: ^0.7.1` | gateway/repository חושפים את כל API הסמנטי ומפרידים exact/fuzzy מ־lexical/hybrid/semantic | ה־BLoC וה־UI אינם מפעילים את המסלול; אין ניהול ארטיפקט, מודל, תאימות או שגיאות משתמש |

### מה PR #3 השלים — ומה לא

PR #3 הוסיף חלקים חשובים:

- `VectorStoreBackend` כחוזה משותף.
- `ZevcStore` שכותב snapshots לדיסק, בודק checksums ונפתח מחדש.
- `IndexPackage` ו־`IndexImporter` עם manifest, SHA-256 והחלפת תיקייה. **דיוק שנוסף ב־S3:**
  ההחלפה אינה אטומית — היא שני renames עם חלון בלי יעד — ולכן נוספו לה שמות דטרמיניסטיים
  ושחזור מפורש.
- `IndexVersion`, פרופילי Fast/Balanced/Best, RRF/weighted/adaptive, thresholds, caches ו־telemetry.
- תיקוני תקינות ל־snapshot/import ושיפורי ranking.

אבל במסלול הפעיל:

- [`SemanticEngine`](src/semantic/engine.rs) מחזיק עדיין `store: VectorStore` ו־`open()` פותח את המימוש הלא־מתמיד.
- [`ZevcStore`](src/semantic/zevc_store.rs) הוא מימוש מקומי חדש, לא הספרייה `zvec`. הוא טוען את כל הווקטורים ל־`HashMap` ומבצע סריקה מלאה `O(N·D)`; אין בו ANN או mmap.
- פורמט החבילה הנוכחי הוא תיקייה עם payloads וקבצי JSON, לא קובץ `.oix` עצמאי.
- אין כלי builder שקורא את ספריית אוצריא או את Tantivy ומפיק ארטיפקט מלא.
- `IndexVersion` לא קשר את החבילה לגרסת corpus/Tantivy, סכמת Tantivy, גרסת סכמת ה־ID, checksum של המודל וזהות backend ה־inference. ✅ **נסגר ב־S3** — ראו למטה.
- ה־importer אינו חשוף דרך `OtzariaHybridEngine`, ה־FFI או אוצריא.
- ה־benchmark החדש מספק כלי מדידה; הוא אינו dataset של רלוונטיות תורנית ואינו הוכחת סקייל על כ־6 מיליון שורות.

### אימות המצב הנוכחי

על `main` הנוכחי עברו:

- `cargo fmt --check`.
- `cargo clippy --all-targets -- -D warnings`.
- `cargo test --lib --tests`: 344 בדיקות פעילות בכל הבינאריים והסוויטות (298 לפני S3).
- `cargo test --lib --tests --features mock-embedding`: 363 בדיקות פעילות.

ב־CI של PR #3 עברו Linux/Windows/macOS ובניית backend ה־inference. בדיקת Golden Vectors עם המודל האמיתי דולגה באותה ריצה, מפני שהסוד `OTZARIA_HF_TOKEN` לא היה זמין.

## 3. מה „מיזוג עם Tantivy” אומר

אין למזג את מסד הווקטורים לתוך Tantivy, ואין להעביר Tantivy למאגר הזה. החיבור הנכון הוא בתוך `otzaria_search_engine`:

1. Tantivy מפיק מועמדים לקסיקליים וציוני BM25.
2. האינדקס הווקטורי מפיק מועמדים סמנטיים.
3. שני המקורות מתחברים על `line_id` הגלובלי השמור ב־Tantivy.
4. ה־fusion, grouping ו־pagination מתרחשים ב־Rust.
5. תוצאה סמנטית נטענת מ־Tantivy לפי `line_id`; רק אז הטקסט וה־metadata חוצים FFI ל־Dart.

סעיפים 1–5 כבר ממומשים ברמה התשתיתית ב־PR #12. הפער החשוב שנשאר הוא **זהות ה־corpus**:

- ה־ID הרשמי באוצריא משלב `catalogue_order` ב־32 הסיביות הגבוהות ואת מספר השורה/הקטע ב־32 הסיביות הנמוכות.
- הארטיפקט הסמנטי חייב להיבנות מאותו סדר קטלוגי, מאותה סכמה ומאותו תוכן מנורמל כמו הארטיפקט של Tantivy.
- מספיק שספר אחד נוסף באמצע הקטלוג כדי ש־ID של ספרים רבים ישתנה. אינדקס סמנטי מגרסה אחרת עלול להצביע לשורות לא נכונות — לא רק לפספס תוצאות.

לכן כל חבילה רשמית חייבת להכיל ולאמת לפני פתיחה:

- `corpus_id` או digest דטרמיניסטי של מסמכי הספרייה הרשמית.
- `tantivy_schema_version`.
- `document_id_scheme_version`.
- גרסת הקטלוג/הספרייה.
- `model_id`, checksum של קובץ המודל, backend, pooling, dimensions, precision, normalization ו־chunking identity.
- checksum של כל payload ושל manifest החבילה.

אם הזהויות אינן תואמות, המסלול הסמנטי צריך להיכשל במפורש ולא לנחש.

## 4. סדר העבודה המומלץ

### S0 — יישור חוזה המוצר ✅ **בוצע**

**מאגר:** `otzaria-semantic-search`.

**עבודה:**

- ✅ להגדיר במפורש official read-only index, ללא user overlay וללא background indexing באפליקציה.
- ✅ להחליף את המונחים `cloud` ב־package/distribution.
- ✅ לעדכן README, `docs/DEVELOPMENT.md`, `docs/CODE_MAP.md` ו־`מפת דרכים.md`, שכרגע סותרים חלקית את קוד PR #3.
- ✅ להכריע כיצד המודל המקומי מופץ, בכפוף לרישיון ה־gated/non-commercial הנוכחי.

**שער קבלה:** אין בתיעוד שלב שטוען שהמשתמש מאנדקס, שיש user overlay, ש־Zevc הוא ANN או שאוצריא כבר מפעילה UI סמנטי.

#### מה נעשה בפועל

| תוצר | היכן |
|---|---|
| חוזה המוצר כמסמך מחייב שגובר על כל תיעוד אחר | [`docs/PRODUCT_CONTRACT.md`](docs/PRODUCT_CONTRACT.md) |
| ארבע החלטות ההיקף גם בקוד, כ־doc ברמת ה־crate | [`src/lib.rs`](src/lib.rs) |
| `cloud` → `distribution`, כולל doc שמסביר למה | [`src/distribution/`](src/distribution/) |
| הכרעת הפצת המודל | [`docs/MODEL_DISTRIBUTION.md`](docs/MODEL_DISTRIBUTION.md) |
| README מיושר לקוד שמוזג ולשלבים S0–S8 | [`README.md`](README.md) |
| מפת קוד שמכסה את מודולי PR #3 | [`docs/CODE_MAP.md`](docs/CODE_MAP.md) |
| מדריך הפיתוח עם סטטוס נכון | [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md) |
| מפת הדרכים סומנה היסטורית, והפריטים שבוטלו נמחקו/סומנו | [`מפת דרכים.md`](מפת%20דרכים.md) |

**מה נמחק מהתיעוד** (וזה עיקר שער הקבלה):

- Phase „Background Streaming Indexer (`StreamSink<IndexingProgress>`)” ו־„Indexing Progress indicator” ב־README.
- צומת `Mutable user-books overlay` בדיאגרמת מפת הדרכים, ו־„overlay קטן לספרי משתמש” ב־P9.
- progress stream ו־cancel/resume ב־P7.
- „אין inference אמיתי” ו־„one chunk → one `embed_one()`” ב־`DEVELOPMENT.md` — שניהם לא נכונים מאז PR #2/#3.
- כל תיאור של `ZevcStore` כ־ANN/mmap/`zvec`, כולל doc comments בקוד (`errors.rs`, `semantic/mod.rs`, `hybrid/mod.rs`).

**הכרעת המודל** (S0, בקצרה): המודל בבעלות פרויקט אוצריא, ולכן ה־gate והרישיון הם
מדיניות שלנו ולא אילוץ חיצוני — אין חסם זכויות. הוכרע: **לא מצורף למתקין** (נפח), ה־crate
לעולם אינו מוריד, ומודל שאינו נטען מותיר את המסלול הסמנטי בלתי־זמין בלי וקטורים מזויפים.
נשאר פתוח במכוון עד S6: האם ההורדה קורית מתוך האפליקציה או כחבילת נתונים נפרדת — שתי
האפשרויות זהות מבחינת חוזה הריצה.

מה שההכרעה **אינה** מספקת, וחשוב לא לבלבל: זהות המודל אינה חלק מזהות החבילה. ה־SHA-256
של קובץ המודל נשמר ב־`SemanticManifest` המקומי בלבד, אין השוואה מול hash מפורסם, ומצבי
`model_missing`/`model_incompatible` אינם קיימים בקוד. שלושתם S3/S5/S6 — הפירוט ב־
[`docs/MODEL_DISTRIBUTION.md`](docs/MODEL_DISTRIBUTION.md) §2.2–2.4.

**אימות:** `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`,
`RUSTDOCFLAGS="-D warnings" cargo doc --no-deps`, ו־298 בדיקות בבנייה רגילה /
317 עם `mock-embedding` — כולם עוברים לאחר שינוי שם המודול.

### S1 — איכות הייצוג ובחירת ממדים

**מאגר:** `otzaria-semantic-search`.

**עבודה:**

- לבנות dataset של שאילות תורניות ותוצאות רלוונטיות מסומנות.
- להשוות `line`, `title + reference + line` ושורות הקשר.
- למדוד 128/256/512/1024 ממדים ו־f32/f16/int8 אם ה־backend תומך.
- למדוד Recall@K, MRR, nDCG וגודל הארטיפקט.
- להקפיא את `embedding_text_version`, הממד, הדיוק, `max_tokens`, pooling ו־normalization כחלק מזהות האינדקס.

**שער קבלה:** החלטה כתובה המביאה את מדדי האיכות, השינוי מול BM25, גודל משוער ל־6.1 מיליון שורות ונימוק לבחירה.

### S2 — backend מתמיד, read-only ובר־סקייל

**מאגר:** `otzaria-semantic-search`.

**עבודה:**

- להפוך את `SemanticEngine` לתלוי ב־`VectorStoreBackend` ולא ב־`VectorStore` קונקרטי.
- להוסיף מצב `official-read-only` שאינו חושף delete/upsert בזמן ריצת האפליקציה.
- להריץ את `ZevcStore` כ־baseline נכונות ב־1M וב־6M רשומות; אם RAM/latency אינם בתקציב, לבחור ANN אמיתי על הדיסק ולא לשנות רק שם.
- לבדוק cold-open, p50/p95/p99, peak RSS וגודל דיסק; ואם נבחר backend מקורב — גם את שגיאת ה־recall שלו.
- לאמת reopen, קובץ קטוע, checksum שגוי, הפרעת חשמל בהתקנה ושני תהליכי קריאה.
- לוודא שהפתיחה אינה טוענת את כל הווקטורים ל־RAM, אלא אם מדידה מוכיחה שזה עומד בתקציב בכל היעדים.

**שער קבלה:** פתיחה מחדש מחזירה `vectors_persisted=true`; חיפוש על corpus בגודל היעד עומד בתקציבי RAM/latency/disk שנקבעו מראש.

### S3 — חוזה ארטיפקט רשמי 🟡 **חלק הזהות בוצע**

**מאגר:** `otzaria-semantic-search`.

**עבודה:**

- ✅ להרחיב את `IndexVersion` בכל זהויות ה־corpus, Tantivy, ה־ID, המודל וה־backend.
- 🟡 להכריע אם `.oix` הוא תיקייה מוגדרת או ארכיון יחיד, ולתעד את החוזה. **הוכרע:**
  תיקייה מוגדרת, עם `manifest.json` + `payloads.json` + payloads. שאלת הארכיון הבודד
  להורדה נדחתה במפורש ל־packer (S4a) ולניהול הקבצים (S6), והיא אינה משנה את החוזה —
  ארכיון כזה ייפרס ל־staging ויאומת באותו סדר.
- 🟡 לוודא שה־manifest וה־payload תואמים. **קיים:** גודל מוצהר **לכל payload בנפרד** (לא רק
  בסכום — אחרת בייטים שעברו מקובץ אחד לאחר לא היו נראים), checksum לכל payload, ודחייה של
  ספירות אפס. **חסר:** ספירת ספרים/וקטורים מול **תוכן** ה־payload, שדורשת קורא של פורמט
  ה־store — S2a/S4a.
- ✅ להפריד builder כותב מ־runtime קורא: `IndexPackage::write` מול
  `verify_for_install`/`verify_for_open`, ו־`VerifiedPackage` כטוקן שאין דרך לעקוף.
- 🟡 להגדיר התקנה שניתן להשתחזר ממנה: אימות המקור, staging עם `fsync`, אימות ה־staging, swap עם גיבוי,
  שחזור וניקוי. הדגל שדילג על אימות הוסר. **מה שאינו אטומי ואין להציג כאטומי:** החלפת
  תיקייה היא בהכרח שני renames (`rename` מסרב להחליף תיקייה לא־ריקה), ובין השניים אין
  תיקיית יעד. החלון הזה **מזוהה ומשוחזר** — שמות דטרמיניסטיים,
  `recover_interrupted_install`, resolver משותף שדוחה יעד בלי שם משלו לפני שנגזר ממנו נתיב,
  וכל כשל אחרי החניית העותק הקודם (כולל `fsync`) עובר דרך יציאה שמחזירה או אומרת איפה
  העותק — ולא נעלם. נבדק בהזרקת כשל ל־rename ול־`fsync`.

**שער קבלה:** ארטיפקט שנבנה במכונה אחת מועתק למכונה נקייה, נפתח ללא כתיבה מחדש, ומסרב להיפתח אם שונה קובץ או זהות אחת.

**מה שהשער עדיין אינו מוכיח, ואין לטעון אחרת:**

1. „נפתח” — אין קורא. האימות נאכף ב־`verify_*`/`import`, אך `SemanticEngine` עדיין פותח
   store בזיכרון ואינו מקבל `VerifiedPackage`. S2a.
2. „מסרב אם שונה קובץ” — בעומק הפתיחה, עריכה **באותו אורך בדיוק** אינה נתפסת, כי גיבוב
   של גיגה־בייטים בכל עלייה אינו בתקציב. בהתקנה כן נתפסת. הבדיקה שחסרה שייכת לקורא
   ה־store. S2a.
3. „הארטיפקט הרשמי” — `payloads.json` נוסע בתוך החבילה, ולכן payload שהוחלף יחד עם
   ה־checksum שלו עקבי עם עצמו. `ArtifactExpectation::with_published_digest` היא המכניזם
   שמבדיל; מי שיפרסם את ה־digest ויחתום עליו הוא S6.
4. אין תקציב **נמדד** לזמן פתיחה או התקנה. S2b/S8.

#### מה נעשה בפועל

| תוצר | היכן |
|---|---|
| חוזה הארטיפקט: שדות הזהות, שני עומקי האימות, עוגן האמון, פרוטוקול ההתקנה, ומה שאינו נאכף | [`docs/ARTIFACT_CONTRACT.md`](docs/ARTIFACT_CONTRACT.md) |
| זהות בשלוש קבוצות — corpus / model / store, 17 שדות | [`src/semantic/versioning.rs`](src/semantic/versioning.rs) |
| כיסוי הזהות מובטח בשתי בדיקות שנגזרות מה־JSON המסוריאלי — לא בטיפוס. שדה שנשמר ואינו מושווה, או שוולידציית השלמות שכחה, מפיל בדיקה | שם |
| דחייה טיפוסית לפי שדה, ולא `bool` | `ArtifactError` ב־[`src/errors.rs`](src/errors.rs) |
| שני עומקים: `verify_for_install` מגבב הכול, `verify_for_open` לא — והטוקן נושא `depth` | [`src/distribution/package.rs`](src/distribution/package.rs) |
| `IndexPackage::digest()` + `ArtifactExpectation` — עוגן אמון חיצוני, וויתור מוצהר בשם | שם |
| `PayloadDescriptor` — SHA-256 **וגודל** לכל payload (metadata גרסה 2), כדי שהפתיחה הזולה תבדוק כל קובץ ולא רק את הסכום | שם |
| שמות payload case-insensitive מול ה־metadata, ודחיית שתי רשומות שנבדלות רק באותיות | שם |
| שמות payload פורטביליים, נבדקים על המחרוזת (allowlist) ולא דרך `Path` | שם |
| `VerifiedPackage` — טוקן שרק שערי האימות מייצרים, כדי שהקורא לא יקבל נתיב לא־מאומת | שם |
| אימות המקור לפני העתקה ואימות ה־staging אחריה; ללא דגל דילוג; `fsync` לקבצים ולתיקייה | [`src/distribution/importer.rs`](src/distribution/importer.rs) |
| `recover_interrupted_install` — שני חלונות הקריסה של ההחלפה, עם failpoints לכשל rename | שם |
| בדיקות דרך ה־API הציבורי בלבד, כמו שיעשה `otzaria_search_engine` | [`tests/artifact_contract.rs`](tests/artifact_contract.rs) |

**מה שהוכרע כאן במכוון בלי S1:** שום ערך לא קופא. הממד, הדיוק, `embedding_text_version`
ו־`store_format_version` הם **שדות**, ומדידת S1 תמלא אותם. מה שכן קופא הוא הדרישה
שיהיו מוצהרים ושיושוו.

**אימות:** `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`,
`RUSTDOCFLAGS="-D warnings" cargo doc --no-deps`, ו־344 בדיקות בבנייה רגילה (מהן 6
בסוויטת חוזה הארטיפקט) / 363 עם `mock-embedding`.

### S4 — builder שמונע סטייה מ־Tantivy

**מאגר:** `otzaria_search_engine`, עם API ב־`otzaria-semantic-search`.

**עבודה:**

- להוסיף CLI/build mode שפותח את אינדקס Tantivy הרשמי הסופי ב־read-only.
- לסרוק את המסמכים השמורים ב־Tantivy לפי `filePath`, ולהעביר ספר אחד בכל פעם ל־semantic builder.
- להשתמש ב־`id`, `sectionId`, `lineHash`, title, reference, text, segment, facets ו־filePath ששמורים ב־Tantivy; אין לבנות את המטא־דאטה שוב ב־Dart.
- לבנות embeddings באצוות, לבנות את מבני האחזור של ה־backend שנבחר ב־S2, ולהפיק manifest/checksums.
- לבצע בדיקת join מלאה: כל `line_id` באינדקס הווקטורי קיים ב־Tantivy, ספירות תואמות, ומדגם אקראי מתאחזר לאותו ספר/מראה מקום/טקסט.

**שער קבלה:** פקודה אחת מקבלת אינדקס Tantivy ומודל, מפיקה ארטיפקט סמנטי מלא, ומאמתת אותו מול אותו Tantivy ללא Dart.

### S5 — חיבור הגרסה הסמנטית החדשה ל־Search Engine

**מאגר:** `otzaria_search_engine`.

**עבודה:**

- לעדכן את ה־pin מ־`d9de678` לגרסה שכוללת את ה־backend הפעיל, חוזה החבילה וה־builder API.
- להתאים את adapter ה־API ל־`profile`/`feature_flags` ולכל שינוי חוזה שנוסף אחרי PR #3.
- להחליף את המסלול הרשמי `semantic_index_books(Vec<...>)` ב־`open/install_official_semantic_index`.
- להשאיר את כל מועמדי BM25, fusion, hydration, grouping ו־pagination באותה קריאת Rust.
- להוסיף סטטוסים מפורשים: `missing`, `installing`, `ready`, `incompatible`, `corrupt`, `model_missing`, `model_incompatible`, `unsupported_platform`.
- לא לבצע fallback שמתחזה להצלחה: hybrid רשאי להידרד ללקסיקלי עם reason; semantic-only מחזיר כשל/תוצאה ריקה מפורשת, ולא BM25 מסווה.
- להפיק בינאריים חתומים חדשים לכל מטריצת הפלטפורמות.

**שער קבלה:** בדיקת Rust ובדיקת Dart/FRB פותחות Tantivy + ארטיפקט סמנטי מוכן, מריצות hybrid ו־semantic-only, ומוכיחות hydration נכון לפי ID. בדיקת mismatch חייבת להיכשל לפני החזרת תוצאות.

### S6 — ניהול הארטיפקט והמודל באוצריא

**מאגר:** `otzaria`.

**עבודה:**

- להגדיר נתיבים יציבים למודל, לחבילה, ל־staging ולגיבוי.
- לבחור אם הקבצים מצורפים להתקנה או מורדים כחבילת נתונים סטטית. בשתי האפשרויות החיפוש עצמו אינו שירות ענן.
- לממש הורדה/התקנה עם שחזור (אותו פרוטוקול שני־ה־renames), checksum, ביטול, resume, מקום דיסק חסר והסרה.
- להשוות את זהות הארטיפקט לאינדקס Tantivy שנפתח בפועל ולגרסת האפליקציה.
- לקרוא `configure/open` פעם אחת באתחול שכבת החיפוש, לא בכל שאילה.
- לא להעביר את מיליוני שורות הספרייה מ־Dart ל־Rust.

**שער קבלה:** התקנה נקייה מגיעה ל־`ready`; restart של האפליקציה אינו מאנדקס דבר; חבילה פגומה או לא תואמת מוצגת בשגיאה ניתנת לתיקון והחיפוש הלקסיקלי נשאר תקין.

### S7 — BLoC ו־UI באוצריא

**מאגר:** `otzaria`.

**עבודה:**

- להוסיף `RetrievalMode`: `lexical`, `hybrid`, `semantic`, בנפרד מ־exact/advanced/fuzzy.
- להעביר את הבקשה דרך `SearchBloc` אל `SearchRepository.searchSemantic` כאשר נבחר hybrid/semantic.
- למפות loading, empty, error, degraded ו־unsupported למצבי UI ברורים.
- להציג מקור תוצאה רק אם הוא מועיל למשתמש; ציונים פנימיים אינם צריכים להיות UI קבוע.
- לשמור העדפה ולהחזיר ברירת מחדל בטוחה אם הפלטפורמה אינה נתמכת.
- לבדוק pagination, grouping, facets, fuzzy, nikud/taamim, history ושחזור טאב בכל שלושת מצבי האחזור.

**שער קבלה:** בדיקת widget/BLoC ובדיקת native מקצה לקצה מוכיחות שכל מצב מפעיל את המנוע הנכון, ושכשל סמנטי אינו מוצג כהצלחה סמנטית.

### S8 — שערי release

**מאגרים:** שלושתם.

**עבודה:**

- בנייה והרצה ב־Windows, Linux, macOS, Android ויעדי Apple הנתמכים.
- קישור אפליקציה אמיתי מול הבינאריים המוכנים מראש, לא רק בניית static library.
- בדיקות עם המודל האמיתי ועם ארטיפקט בגודל ייצוגי.
- תקציבי cold start, query p50/p95/p99, peak RAM, דיסק וזמן התקנה.
- התאוששות מהורדה חלקית, אינדקס פגום, mismatch, חוסר מקום, מודל חסר ועדכון ספרייה.
- חסימת release אם בדיקות native/real-model דולגו במקום לרוץ.

**שער קבלה:** release candidate עובר את מטריצת היעדים ואת תקציבי האיכות/המשאבים, וניתן להתקין אותו על מכונה נקייה ללא אינדוקס מקומי.

## 5. פריסת PRים מומלצת

| PR | מאגר | תוצר מרכזי | תלות |
|---|---|---|---|
| A | `otzaria-semantic-search` | יישור התיעוד (✅ S0), קיבוע corpus/index contract ותכנית המדדים (S1) | אין |
| B | `otzaria-semantic-search` | חיבור `VectorStoreBackend` ל־engine ו־read-only persistent backend | A |
| C | `otzaria-semantic-search` | ✅ חוזה package/versioning בטוח (S3, ענף `s3-artifact-contract`) | אין — נעשה לפני B |
| D | `otzaria_search_engine` | כלי builder מ־Tantivy ובדיקת corpus join | C |
| E | `otzaria_search_engine` | repin של ה־crate, open/install API, FFI ובינאריים חדשים | B–D |
| F | `otzaria` | שירות ארטיפקט/מודל ואתחול המנוע | E |
| G | `otzaria` | BLoC, UI, הגדרות ובדיקות widget/native | F |
| H | שלושתם | מדידות מלאות, מטריצת פלטפורמות ו־release candidate | A–G |

**מצב נוכחי:** S0 הושלם (החלק התיעודי/חוזי של PR A), ואחריו נעשה PR C — חוזה הזהות של
S3. הסדר הוחלף במכוון: C אינו תלוי ב־A/B, מפני שהוא קובע אילו שדות חייבים להיות מוצהרים
ולא אילו ערכים ייבחרו. מה שנשאר ב־A הוא S1 — dataset האיכות והחלטת הממד/הדיוק, כלומר
מדידה ולא הכרעת חוזה.

**הבא בציר המימוש:** PR B (S2a) — לחבר את `SemanticEngine` ל־`VectorStoreBackend`
ולפתוח ארטיפקט מאומת read-only, כלומר לתת ל־`VerifiedPackage` קורא. אחריו D/E, ואז
F/G באוצריא.

## 6. מה אינו דרוש כרגע

הפריטים הבאים יוצאים מה־MVP הנוכחי:

- אינדוקס ברקע אצל המשתמש.
- progress stream ו־cancel/resume של embedding הספרייה.
- overlay ניתן לכתיבה לספרים אישיים.
- diff/upsert פר־ספר בזמן ריצת אוצריא.
- API שמעביר מיליוני `SemanticBookInput` מ־Dart.
- שרת שמקבל את שאילות או מבצע את החיפוש.

## 7. ההגדרה ל־MVP מושלם

ה־MVP מושלם רק כאשר:

1. מכונת build מפיקה Tantivy וחבילת וקטורים תואמת מאותה מהדורת ספרייה.
2. אוצריא מתקינה/מאתרת את החבילה ואת המודל, בודקת זהות ופותחת אותם מקומית.
3. חיפוש semantic מייצר רק את וקטור השאילה, מאחזר מועמדים מהארטיפקט ומבצע hydration מ־Tantivy.
4. חיפוש hybrid מאחד BM25 ווקטורים ב־Rust לפני pagination.
5. restart אינו בונה את האינדקס מחדש ואינו טוען את כולו ל־RAM מעבר לתקציב.
6. ה־UI מפעיל lexical/hybrid/semantic בפועל ומציג מצב חסר/פגום/לא־תואם במפורש.
7. מדדי האיכות, הזמן, ה־RAM והדיסק עוברים את שערי ה־release בכל הפלטפורמות הנתמכות.

עד אז הניסוח המדויק הוא: **ה־FFI וה־Tantivy integration קיימים כתשתית; ארטיפקט הווקטורי הרשמי, הפתיחה המתמידה והפעלת המוצר ב־UI עדיין חסרים.**
